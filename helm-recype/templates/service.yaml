apiVersion: v1
kind: Service
metadata:
  name: {{ default .Values.deploymentName (include "helm-recyoe.fullname" .) }}-service
  labels:
    app: {{ default .Values.deploymentName (include "helm-recyoe.fullname" .) }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ default .Values.deploymentName (include "helm-recyoe.fullname" .) }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.container.port }}
